# schemas

Zod schemas for every content type in `/src/data`. Run by `scripts/validateContent.ts` at CI time and by the runtime content loader at startup. Schema mismatches fail loudly in dev, warn in prod.
