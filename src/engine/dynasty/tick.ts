/**
 * Dynasty monthly tick — v0.1 stub.
 *
 * Real implementation lands in Issue #18 (aging, mortality, succession).
 * For now we expose the function so the tickEngine orchestrator can
 * call it.
 */

export function dynastyTick(): void {
  // Intentional no-op for the v0.1 skeleton.
}
