/**
 * Military monthly tick — v0.1 stub.
 *
 * Real implementation lands in Issue #19 (army movement + abstract
 * combat). For now we expose the function so the tickEngine
 * orchestrator can call it.
 */

export function militaryTick(): void {
  // Intentional no-op for the v0.1 skeleton.
}
