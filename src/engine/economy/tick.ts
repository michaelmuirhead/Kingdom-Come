/**
 * Economy monthly tick — v0.1 stub.
 *
 * Real implementation lands in Issue #17 (province tax income + manpower
 * regeneration). For now we expose the function so the tickEngine
 * orchestrator can call it.
 */

export function economyTick(): void {
  // Intentional no-op for the v0.1 skeleton.
}
