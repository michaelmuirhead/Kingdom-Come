# ai

AI decision dispatcher, personality profile derivation, and per-system AI handlers (war, diplomacy, reform, estate). AI uses the same simulation surface as the player, so decisions produce the same state shape player decisions produce.
