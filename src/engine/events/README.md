# events

Event lifecycle: condition checking, scheduling (mtth), resolution. Conditions and effects are registered in central handler maps so content authors can add events in JSON without touching engine code. See TECH.md Section 9.
